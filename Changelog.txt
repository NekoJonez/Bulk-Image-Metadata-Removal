v0.2 - 25/12/2025

- Improved the options section to make it easier to add features in the future.
- The options now use the correct mouse cursor.
- Rewrote the search for ExifTool to be more dynamic.
- Removed a double space in the UI in the ExifTool listing.
- Rewrote the UI itself so it's more dynamic.
- Fixed a bug where when you resized the window, the logbox went over the credit text and the exit button.
- Fixed a bug where the "remember" button for the ExifTool location became unuseable after a conversion.
- Fixed a bug where the "exit" button was still clickable during a conversion process.
- Added a label to the options section for more UI clarity.
- Started to improve variable names for more reading clarity.
- Improved the code comments.
- Improved the header.
- The tool now default also searches the images folder for the ExifTool.
- Gave the tool an icon.

v0.3 - 07/03/2026

- Very minor linting changes.
- The tool will now create an INI file saving your settings.
- A new button so can choose to remember your settings.
- Removed some unneeded enters.
- Added comments to the functions that were missing them.
- The outline of the bottom of the UI has been corrected.

v0.3.1 - 08/03/2026 (HOTFIX)

- Fixed an issue where saving options stopped working due to the conversion to an EXE.

v0.4 - 25/03/2026

- Added a forgotten comment within the v0.3 release
- The tool will now correctly make the INI file in AppData and not in the default Powershell install folder.
- Tweaked the tooltips a tiny bit.
- Minor spaces added to make the writing of the functions consistent.
- Fixed a bug where in some cases the unchecking "save settings" checkbox didn't work.
- Added an "Open..." folder button to the input and output folders.
